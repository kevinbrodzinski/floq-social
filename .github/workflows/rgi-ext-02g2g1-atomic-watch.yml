name: RGI EXT 02G2G1 ATOMIC A4000 WATCH — SUPERSEDED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'RGI-EXT-02G.2g.1 superseded by prospectively frozen RGI-EXT-02G.2g.3; no provider query or provisioning authorized here.'
